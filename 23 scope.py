# the region that the variable is recognized . A variable is only avaliable from inside the region it is created
# A global and locally scoped versions of a variable can be created


def display_name():
    name = "poki"
    print(name)


display_name()
# print(name)
